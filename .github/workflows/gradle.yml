name: 유닛 테스트 자동화

on: 
  push:
    branches:
      - main
      - feature/*
  pull_request:
    branches:
      - main
      - feature/*

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - name: 리포지토리 가져오기
      uses: actions/checkout@v4
    
    - name: JDK 17 설치
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    # - name: Setup Gradle
    #   uses: gradle/actions/setup-gradle@dbbdc275be76ac10734476cc723d82dfe7ec6eda # v3.4.2

    - name: Gradle build를 위한 권한 부여
      run: chmod +x gradlew
      
    - name: Gradle build 실행
      run: ./gradlew build

    # - name: 테스트 실패시, 실패한 코드 라인의 Check 코멘드 등록
    #   uses: mikepenz/action-junit-report@v3
    #   if: always()
    #   with:
    #     repost_paths

